

## **Fonksiyon Nedir?**

Belirli bir işi yapan, tekrar kullanılabilir kod parçacıklarıdır.  
Avantajları: modülerlik, okunabilirlik, hata tespiti kolaylığı.

- **Parametre:** Fonksiyonun aldığı girdi.
    
- **Argüman:** Fonksiyon çağrılırken verilen değer.
    
- **return:** Değer döndürür.
    

---

## **1. def ile Fonksiyon Tanımlama**

Genel yapı:

```python
def fonk_adi(parametreler):
    işlemler
    return değer
```

Örnek:

```python
def selamVer():
    print("Merhaba!")
```

Parametreli:

```python
def adaGoreSelamVer(isim):
    print(isim, "Merhaba!")
```

Birden fazla parametre:

```python
def adSoyad_SelamVer(isim, soyisim):
    print(isim, soyisim, "Merhaba!")
```

---

## **2. Return ile Değer Döndürme**

Tek değer:

```python
def toplam(a, b):
    return a + b
```

Birden fazla değer (tuple):

```python
def toplam_fark(a, b):
    return a+b, a-b
```

---

## **3. Varsayılan Parametre**

```python
def ciftSayilariGetir(n, bas=0):
	sayilar = []
	while len(sayilar) < n:
		if bas % 2 == 0:
			sayilar.append(bas)
		bas += 1
	return(sayilar)
```

Çağrı:

```python
ciftSayilariGetir(5) # [0, 2, 4, 6, 8] 
ciftSayilariGetir(5, 20) # [20, 22, 24, 26, 28]
```

---

## **4. lambda Fonksiyonları**

Tek satırlık fonksiyonlar.

Örnek:

```python
toplam = lambda a,b: a+b
tek_mi = lambda a: a % 2 == 1
```

---

## **5. map() ve filter()**

```python
liste = [0,1,2,3,4,5]

sonuc = map(lambda x: x*10, liste)
filtre = filter(lambda x: x % 2 == 1, liste)
```

---

## **6. Örnek Fonksiyon Problemleri**

**Asal sayı testi:**

```python
def asal_mi(n):
    if n >= 2:
	    for i in range(2, n):
		    if sayi % i == 0:
			    return False
			    break
			else:
				return True
	else:
		return False
```

**Sayı yer değiştirme:**

```python
def degistir(a,b):
    return b,a
```

**Metni ters çevirme:**

```python
def tersCevir(metin):
    return metin[::-1]
```

**Sesli harf sayısı:**

```python
def sesliHarfSayisi(metin):
    ...
```

**Tekrarlayan karakterleri kaldırma:**

```python
def tekrarlari_kaldir(metin):
    ...
```

**Fibonacci listesi:**

```python
def fibonacciSerisi(n):
    ...
```

**Liste eleman tipi kontrolü ve toplam:**

```python
def listeKontrol(liste):
    ...
```

**Çember alan ve çevre (varsayılan pi):**

```python
def hesapla(r, pi=3.14):
    return 2*pi*r, pi*r*r
```

---

İstersen bu iki özetin birleşik tek bir PDF veya tek bir uzun MD halinde versiyonunu da hazırlayabilirim.